[defaults]
# Disable host key checking to avoid interactive prompts on first connection.
host_key_checking = False

[privilege_escalation]
# These settings explicitly tell Ansible how to use sudo.
become = True
become_method = sudo
become_user = root
become_ask_pass = False
